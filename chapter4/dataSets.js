let age = 25; // Primitive (number)
let name = "Alice"; // Primitive (string)
let isLoggedIn = true; // Primitive (boolean)

let colors = ["red", "green", "blue"]; // Array (data set) - holds strings (primitives)
let person = {
  firstName: "Bob",
  lastName: "Smith",
  age: 30  // Can include primitives and other data structures
}; // Object (data set) - holds strings (primitives) and a number (primitive)